@model SyndicateIT.DataLayer.DataContext.T_Document_Form_Category

@{
    ViewBag.Title = "Create";
}
<div id="content">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-title txt-color-blueDark">
                Create Form Category
                @Html.ActionLink("Back to List", "Index", null, new { style = "float:right" })
            </h1>
        </div>
    </div>
    <div class="row">
        @using (Html.BeginForm("Create", "Document_Form_Category", FormMethod.Post, new { @class = "", role = "form" }))
        {
            <fieldset>
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group  col-sm-6">
                    @Html.LabelFor(model => model.Title, htmlAttributes: new { @class = "control-label" })
                    @Html.EditorFor(model => model.Title, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Title, "", new { @class = "text-danger" })
                </div>

                <div class="form-group">
                    <div class="col-md-12">
                        <input type="submit" value="Create" class="btn btn-default" />
                    </div>
                </div>
            </fieldset>
        }
    </div>
</div>
